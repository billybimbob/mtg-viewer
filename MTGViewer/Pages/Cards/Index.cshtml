@page
@model Cards.IndexModel
@{
    ViewData["Title"] = "Card Collection";
}

@* <component type="typeof(Library)" 
    render-mode="ServerPrerendered" 
    param-IsSignedIn="@Model.IsSignedIn" /> *@

@(await Html.RenderComponentAsync<Library>(
    RenderMode.ServerPrerendered, new { Model.IsSignedIn, Model.PageSize }))
@page
@model MTGViewer.Pages.Cards.IndexModel

@{
    ViewData["Title"] = "Collection";
}

<component type="typeof(Library)" 
    render-mode="ServerPrerendered" 
    param-IsSignedIn="@Model.IsSignedIn" />

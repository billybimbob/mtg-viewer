@page
@model IndexModel
@{
    ViewData["Title"] = "MTG Viewer";
    //code blocks not affected by rendering, asterisk blocks are for rendering
}

<component type="typeof(Home)" render-mode="ServerPrerendered"/>
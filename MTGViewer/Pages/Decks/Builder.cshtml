@page "{id:int?}"
@model Decks.BuilderModel
@{
    string title = Model.DeckName is null
        ? "New Deck"
        : $"{Model.DeckName} - Builder";

    ViewData["Title"] = title;
}

@* <component type="typeof(Components.DeckBuilder)" 
    render-mode="ServerPrerendered" 
    param-User="@Model.CardUser"
    param-DeckId="@Model.DeckId" /> *@

@(await Html.RenderComponentAsync<DeckBuilder>(
    RenderMode.Server,
    new { Model.UserId, Model.DeckId, Model.PageSize }))
